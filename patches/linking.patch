diff -ruN -x '*.pyc' a/linking/ttypes.py b/linking/ttypes.py
--- a/linking/ttypes.py	2015-09-26 07:29:58.875565000 -0400
+++ b/linking/ttypes.py	2015-09-26 07:59:51.266739000 -0400
@@ -121,6 +121,14 @@
     return
 
 
+  def __hash__(self):
+    value = 17
+    value = (value * 31) ^ hash(self.confidence)
+    value = (value * 31) ^ hash(self.targetId)
+    value = (value * 31) ^ hash(self.dbId)
+    value = (value * 31) ^ hash(self.dbName)
+    return value
+
   def __repr__(self):
     L = ['%s=%r' % (key, value)
       for key, value in self.__dict__.iteritems()]
@@ -209,6 +217,12 @@
     return
 
 
+  def __hash__(self):
+    value = 17
+    value = (value * 31) ^ hash(self.sourceId)
+    value = (value * 31) ^ hash(self.linkTargetList)
+    return value
+
   def __repr__(self):
     L = ['%s=%r' % (key, value)
       for key, value in self.__dict__.iteritems()]
@@ -297,6 +311,12 @@
     return
 
 
+  def __hash__(self):
+    value = 17
+    value = (value * 31) ^ hash(self.metadata)
+    value = (value * 31) ^ hash(self.linkList)
+    return value
+
   def __repr__(self):
     L = ['%s=%r' % (key, value)
       for key, value in self.__dict__.iteritems()]
